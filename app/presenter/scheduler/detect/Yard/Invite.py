from hiworker import *


class DetectInvite(DetectImage):
    def __init__(self):
        super(DetectInvite, self).__init__()

    def is_invite_evo_fire_stage_1(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_1", "invite_area")
        return result

    def is_invite_evo_fire_stage_2(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_2", "invite_area")
        return result

    def is_invite_evo_fire_stage_3(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_3", "invite_area")
        return result

    def is_invite_evo_fire_stage_4(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_4", "invite_area")
        return result

    def is_invite_evo_fire_stage_5(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_5", "invite_area")
        return result

    def is_invite_evo_fire_stage_6(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_6", "invite_area")
        return result

    def is_invite_evo_fire_stage_7(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_7", "invite_area")
        return result

    def is_invite_evo_fire_stage_8(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_8", "invite_area")
        return result

    def is_invite_evo_fire_stage_9(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_9", "invite_area")
        return result

    def is_invite_evo_fire_stage_10(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_fire_stage_10", "invite_area")
        return result

    def is_invite_evo_wind_stage_1(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_1", "invite_area")
        return result

    def is_invite_evo_wind_stage_2(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_2", "invite_area")
        return result

    def is_invite_evo_wind_stage_3(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_3", "invite_area")
        return result

    def is_invite_evo_wind_stage_4(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_4", "invite_area")
        return result

    def is_invite_evo_wind_stage_5(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_5", "invite_area")
        return result

    def is_invite_evo_wind_stage_6(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_6", "invite_area")
        return result

    def is_invite_evo_wind_stage_7(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_7", "invite_area")
        return result

    def is_invite_evo_wind_stage_8(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_8", "invite_area")
        return result

    def is_invite_evo_wind_stage_9(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_9", "invite_area")
        return result

    def is_invite_evo_wind_stage_10(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_wind_stage_10", "invite_area")
        return result

    def is_invite_evo_water_stage_1(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_1", "invite_area")
        return result

    def is_invite_evo_water_stage_2(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_2", "invite_area")
        return result

    def is_invite_evo_water_stage_3(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_3", "invite_area")
        return result

    def is_invite_evo_water_stage_4(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_4", "invite_area")
        return result

    def is_invite_evo_water_stage_5(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_5", "invite_area")
        return result

    def is_invite_evo_water_stage_6(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_6", "invite_area")
        return result

    def is_invite_evo_water_stage_7(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_7", "invite_area")
        return result

    def is_invite_evo_water_stage_8(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_8", "invite_area")
        return result

    def is_invite_evo_water_stage_9(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_9", "invite_area")
        return result

    def is_invite_evo_water_stage_10(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_water_stage_10", "invite_area")
        return result

    def is_invite_evo_thunder_stage_1(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_1", "invite_area")
        return result

    def is_invite_evo_thunder_stage_2(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_2", "invite_area")
        return result

    def is_invite_evo_thunder_stage_3(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_3", "invite_area")
        return result

    def is_invite_evo_thunder_stage_4(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_4", "invite_area")
        return result

    def is_invite_evo_thunder_stage_5(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_5", "invite_area")
        return result

    def is_invite_evo_thunder_stage_6(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_6", "invite_area")
        return result

    def is_invite_evo_thunder_stage_7(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_7", "invite_area")
        return result

    def is_invite_evo_thunder_stage_8(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_8", "invite_area")
        return result

    def is_invite_evo_thunder_stage_9(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_9", "invite_area")
        return result

    def is_invite_evo_thunder_stage_10(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_evo_thunder_stage_10", "invite_area")
        return result

    def is_invite_orochi_stage_1(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_1", "invite_area")
        return result

    def is_invite_orochi_stage_2(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_2", "invite_area")
        return result

    def is_invite_orochi_stage_3(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_3", "invite_area")
        return result

    def is_invite_orochi_stage_4(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_4", "invite_area")
        return result

    def is_invite_orochi_stage_5(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_5", "invite_area")
        return result

    def is_invite_orochi_stage_6(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_6", "invite_area")
        return result

    def is_invite_orochi_stage_7(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_7", "invite_area")
        return result

    def is_invite_orochi_stage_8(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_8", "invite_area")
        return result

    def is_invite_orochi_stage_9(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_9", "invite_area")
        return result

    def is_invite_orochi_stage_10(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_10", "invite_area")
        return result

    def is_invite_orochi_stage_11(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_orochi_stage_11", "invite_area")
        return result

    def is_invite_himiko_stage_1(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_himiko_stage_1", "invite_area")
        return result

    def is_invite_himiko_stage_2(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_himiko_stage_2", "invite_area")
        return result

    def is_invite_himiko_stage_3(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_himiko_stage_3", "invite_area")
        return result

    def is_invite_chapter_stage_1(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_1", "invite_area")
        return result

    def is_invite_chapter_stage_2(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_2", "invite_area")
        return result

    def is_invite_chapter_stage_3(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_3", "invite_area")
        return result

    def is_invite_chapter_stage_4(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_4", "invite_area")
        return result

    def is_invite_chapter_stage_5(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_5", "invite_area")
        return result

    def is_invite_chapter_stage_6(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_6", "invite_area")
        return result

    def is_invite_chapter_stage_7(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_7", "invite_area")
        return result

    def is_invite_chapter_stage_8(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_8", "invite_area")
        return result

    def is_invite_chapter_stage_9(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_9", "invite_area")
        return result

    def is_invite_chapter_stage_10(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_10", "invite_area")
        return result

    def is_invite_chapter_stage_11(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_11", "invite_area")
        return result

    def is_invite_chapter_stage_12(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_12", "invite_area")
        return result

    def is_invite_chapter_stage_13(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_13", "invite_area")
        return result

    def is_invite_chapter_stage_14(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_14", "invite_area")
        return result

    def is_invite_chapter_stage_15(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_15", "invite_area")
        return result

    def is_invite_chapter_stage_16(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_16", "invite_area")
        return result

    def is_invite_chapter_stage_17(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_17", "invite_area")
        return result

    def is_invite_chapter_stage_18(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_18", "invite_area")
        return result

    def is_invite_chapter_stage_19(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_19", "invite_area")
        return result

    def is_invite_chapter_stage_20(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_20", "invite_area")
        return result

    def is_invite_chapter_stage_21(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_21", "invite_area")
        return result

    def is_invite_chapter_stage_22(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_22", "invite_area")
        return result

    def is_invite_chapter_stage_23(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_23", "invite_area")
        return result

    def is_invite_chapter_stage_24(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_24", "invite_area")
        return result

    def is_invite_chapter_stage_25(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_25", "invite_area")
        return result

    def is_invite_chapter_stage_26(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_26", "invite_area")
        return result

    def is_invite_chapter_stage_27(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_27", "invite_area")
        return result

    def is_invite_chapter_stage_28(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_chapter_stage_28", "invite_area")
        return result

    def find_invite_accpet_button(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_accpet_button", "invite_area")
        return result, coord

    def find_invite_accpet_auto_button(self):
        result, coord, max_similarity = self.find_in_different_template_rect("invite_is_invite_accpet_auto_button", "invite_area")
        return result, coord

    def is_confirm_auto_accept_panel(self):
        result, coord, max_similarity = self.find_in_template_rect("invite_is_confirm_auto_accept_panel")
        return result

    def is_confirm_auto_accept_panel_checked(self):
        result, coord, max_similarity = self.find_in_template_rect("invite_is_confirm_auto_accept_panel_checked")
        return result

    def is_confirm_auto_accept_panel_button(self):
        result, coord, max_similarity = self.find_in_template_rect("invite_is_confirm_auto_accept_panel_button")
        return result
